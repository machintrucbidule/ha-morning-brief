"""Stub — Phase 3 will populate. See MORNING_BRIEF_SPEC.md Section 8."""

import pytest

pytestmark = pytest.mark.skip(reason="stub — populated in Phase 3")


def test_placeholder() -> None:
    assert True
